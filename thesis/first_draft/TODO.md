# Εκκρεμότητες διπλωματικής

Κατάσταση build: 0 σφάλματα, 0 undefined, 93 σελίδες.
Τελευταία ενημέρωση: 20/09/2026.

---

## 1. Κείμενο που λείπει

Σε όλο το `main.tex` δεν υπάρχει άλλο `[προς συμπλήρωση]` πέρα από αυτά τα τρία.
Και τα 8 κεφάλαια και τα 2 παραρτήματα έχουν πλήρες κείμενο.

- [ ] Ευχαριστίες (`main.tex:86`)
- [ ] Ελληνική περίληψη + λέξεις κλειδιά (`main.tex:105`, `main.tex:108`)
- [ ] Extensive English summary + keywords (`main.tex:128`, `main.tex:131`)

---

## 2. Σχήματα και πίνακες placeholder (13)

### Δεν χρειάζονται το ρομπότ, μπορούν να γίνουν τώρα

- [ ] `fig:sim-no-gravity` (§7.1) — στιγμιότυπο του ρομπότ μετέωρου στο PyBullet
- [ ] `fig:sim-results-plots` (§7.2) — τροχιά και roll/pitch από την προσομοίωση
- [ ] `tab:experiment-design` (§7.1) — χρειάζονται σενάρια και αριθμός επαναλήψεων

### Χρειάζονται μέτρηση ή φωτογραφία στο ρομπότ

- [ ] `fig:leg-fk-schematic` (κεφ. 3) — φωτογραφία ποδιού με θ₁, θ₂, θ₃
- [ ] `fig:moving-average-plot` (§6.5.4) — roll/pitch πριν και μετά τον κινητό μέσο
- [ ] `fig:sensor-noise-plot` (§6.5.4) — ακατέργαστο έναντι φιλτραρισμένου σήματος
- [ ] `fig:pid-realtime-response` (§6.6) — απόκριση roll/pitch σε πραγματικό χρόνο
- [ ] `fig:log-plotter-sample` (§6.8) — δείγμα γραφήματος από `log_plotter.py`
- [ ] `fig:real-robot-gait-frames` (§7.3) — καρέ βάδισης και στροφής + γραφήματα
- [ ] `tab:gait-performance-metrics` (§7.3) — ταχύτητα, RMS σφάλμα roll/pitch
- [ ] `fig:controller-response-comparison` (§7.4) — με και χωρίς σταθεροποίηση
- [ ] `tab:stabilization-comparison` (§7.4) — μετρικές με και χωρίς σταθεροποίηση
- [ ] `fig:video-qr` (§7.5) — QR code προς το βίντεο επίδειξης

### Σύνδεσμοι βίντεο (§7.5, 8 θέσεις)

Κάθε bullet έχει δύο κενά, ένα για το πραγματικό ρομπότ και ένα για την προσομοίωση.

- [ ] trot προς όλες τις κατευθύνσεις (εμπρός, πίσω, δεξιά, αριστερά, αυθαίρετη)
- [ ] στροφή επί τόπου
- [ ] στροφή ενώ κινείται εμπρός
- [ ] εκκίνηση με ομαλή μετάβαση στην αρχική στάση

---

## 3. Ασυμφωνίες αριθμών προς επίλυση

- [ ] **Κέρδη PID.** Ο πίνακας του §4.5 δίνει κοινά `0,4 / 0,025 / 0,05`. Το
  Παράρτημα Β δίνει ανά άξονα `roll 0,50 / 0,025 / 0,06` και
  `pitch 0,40 / 0,050 / 0,03`, όπως τρέχουν στο `hw/quad_controller.py`.
  Πρέπει να επιλεγεί ποιο ισχύει και να συμφωνήσουν τα δύο σημεία.
- [ ] **Σχ. 4.9, πριονωτό σήμα φάσης.** Σχεδιάστηκε με τα defaults της
  συνάρτησης (`v = 0,30` m/s, `T_sw = 0,25` s) και δίνει `T_cyc = 0,45` s,
  `β = 0,44`. Το Παράρτημα Β έχει τις τιμές του τηλεχειρισμού,
  `T_cyc = 0,46` s, `β = 0,565`. Είτε ξαναπαράγεται το σχήμα με τις τιμές του
  τηλεχειρισμού, είτε η λεζάντα λέει ότι οι τιμές είναι ενδεικτικές.

---

## 4. Τεχνικά πριν την παράδοση

- [ ] **Μέγεθος PDF: 33 MB.** Οφείλεται σε τρεις φωτογραφίες:
  `downview_hw.jpg` 9,5 MB, `robot.jpg` 7,8 MB, `rubber_ball_feet.jpg` 6 MB.
  Συμπίεση στα ~2000 px το ρίχνει κάτω από 5 MB χωρίς ορατή διαφορά.
- [ ] **21 overfull hboxes**, όλα URL στη βιβλιογραφία. Κοσμητικό.
- [ ] **`images/Controller-Mapping-Templates/` και το zip 13 MB** είναι untracked
  μέσα στα images και θα μπουν στο repo σε ένα `git add`.
- [ ] **Merge `main` στο `thesis`.** Το branch `thesis` είναι πίσω στον κώδικα,
  και τα `hw/quad_controller.py`, `hw/teleop.py` έχουν τοπικές αλλαγές. Θέλει
  merge πριν παγώσουν οι αριθμοί του Παραρτήματος Β.
- [ ] **Edge case στη `config_angle`** του `hw/teleop.py`: με τον μοχλό ακριβώς
  στον κατακόρυφο άξονα (`LX == 0`) η γωνία μένει π αντί για 0. Αν διορθωθεί,
  αλλάζει και η εξίσωση του §6.7.

---

## Ολοκληρωμένα

Κεφάλαια 1 έως 8 και Παραρτήματα Α, Β γραμμένα και ελεγμένα. Όλα τα σχήματα και
οι πίνακες που υπάρχουν έχουν παραπομπή στο κείμενο. Τα σχήματα παράγονται από
τα scripts στο `graphs_for_thesis/`.
